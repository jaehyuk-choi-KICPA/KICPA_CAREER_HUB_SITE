# 런타임 산출물(매 실행 생성)
feed.json
loop.log
# state.json = 마감일 캐시 → 저장소에 보존(Actions가 갱신)해 KICPA 상세 재요청 최소화

# 정찰/검증용 임시 파일(_ 접두 — __init__.py는 영향 없음)
_*.json
_*.html
_*.png
_*.txt
_*.log

# 임베딩 캐시(재생성 가능, 키 없으면 생략)
news_vectors.json

# 카나리아: 드리프트 플래그는 런타임 전용(canary_state.json·canary_report.md는 추적)
canary_drift.flag
# 신선도 모니터: 플래그는 런타임 전용(freshness_report.md·freshness_shot.png는 PR로 추적)
freshness_drift.flag
# 사이트 종단 점검: 런타임 산출물(이슈로 알림 — 커밋 안 함)
sitecheck_fail.flag
sitecheck_report.md
sitecheck_shot.png
sitecheck_result.json

# 비밀 — API 키 등(절대 커밋 금지)
.env
.env.*
*.key
*.pem
.secrets/
.playwright-mcp/

# 파이썬
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
_review/
review-*.png

# Claude Code 개인 로컬 설정(권한 등) — 커밋 제외
.claude/settings.local.json
