# 카나리아 자기검증 리포트 — 🚨 드리프트 감지
_2026-06-19 12:37 · 시각점검(LLM): ON_

| 소스 | 수집 | 어제 | 화면-신입(LLM) | 상태 |
|---|---:|---:|---:|---|
| 구인(수습CPA) | 17 | 18 | 9 | OK |
| 구인(CPA) | 42 | 44 | 6 | OK |
| 삼정KPMG | 3 | 2 | 4 | OK |
| 딜로이트안진 | 20 | 20 | 3 | OK |
| EY한영 | 23 | 20 | 0 | 🚨 화면상 정상 목록 아님: 채용공고 목록이 아닌 EY 한영 소개/랜딩 페이지로 개별 공고 항목이 없음 |
| 삼일PwC | 10 | 10 | 9 | OK |
| 필터 누출(경력 전용 공고) | 71 | - | - | OK |

## 수정 제안 (사람 검토용 — 확정 아님)

### EY한영 (`hanyoung`)
- 사유: 화면상 정상 목록 아님: 채용공고 목록이 아닌 EY 한영 소개/랜딩 페이지로 개별 공고 항목이 없음
- 화면 메모: 채용공고 목록이 아닌 EY 한영 소개/랜딩 페이지로 개별 공고 항목이 없음
- LLM 제안:

> ## 진단
> 
> 제공된 HTML은 클라이언트 렌더링 셸(Next.js 부트스트랩)일 뿐 공고 목록 마크업이 없어 **셀렉터 깨짐과는 무관**합니다. 어댑터는 HTML이 아니라 JSON API에 의존하므로, 0건의 원인은 **API 호출/스키마 측 변화**일 가능성이 큽니다. 다만 라이브 트래픽이 없으니 아래는 모두 **추정**입니다.
> 
> ## 수정 방향 제안 (검토 필요)
> 
> 1. **빌드 경로 변경 정황**: 라우트가 `/career/[path]`(=`/career/home`)로 보임 → 어댑터의 상세 URL `/career/jobs/{sn}`이 `/career/{path}` 계열로 바뀌었는지 확인(추정).
> 2. **API 응답 키 변경 추정**: `data["list"]`/`pagination.totalPages`가 `content`/`data.content` 등으로 바뀌면 `items=[]`로 즉시 종료됨 → 실제 응답 JSON 키를 덤프해 확인.
> 3. **요청 본문/필드명 변경 추정**: `pageableRq`·`openStatusList` 등 필드명이 바뀌면 400/빈 결과 → 브라우저 네트워크 탭에서 실제 POST 바디·엔드포인트 경로 재캡처.
> 4. **헤더 검증 강화 추정**: `prefix` 외 인증/토큰 헤더가 추가됐을 수 있음 → 401/403이 `except`에 흡수돼 0건처럼 보일 가능성, 예외를 한 번 로깅해 상태코드 확인.
> 5.

---
**이 PR은 자동 생성된 *진단/제안*입니다.** 코드 수정은 Claude Code로 직접 검토·보완 후 머지하세요 (자동 머지 금지 — Human-in-the-loop).
